# Agentic Flow - Multi-Model Configuration Template
# Run multiple model providers simultaneously
# Deploy multiple services with different configurations

# === Service 1: Claude (Premium Quality) ===
# Use for complex reasoning, code reviews, architecture
CLAUDE_MODEL_PROVIDER=anthropic
CLAUDE_MODEL_NAME=claude-3-5-sonnet-20241022
CLAUDE_ANTHROPIC_API_KEY=your-anthropic-api-key-here
CLAUDE_PORT=8080

# === Service 2: OpenRouter/Llama (Cost-Effective) ===
# Use for simple tasks, research, documentation
OPENROUTER_MODEL_PROVIDER=openrouter
OPENROUTER_MODEL_NAME=meta-llama/llama-3.1-8b-instruct
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_PORT=8081

# === Service 3: DeepSeek (Code Specialist) ===
# Use for code generation and analysis
DEEPSEEK_MODEL_PROVIDER=openrouter
DEEPSEEK_MODEL_NAME=deepseek/deepseek-chat-v3.1
DEEPSEEK_OPENROUTER_API_KEY=your-openrouter-api-key-here
DEEPSEEK_PORT=8082

# === Service 4: ONNX (Fully Local) ===
# Use for offline/air-gapped environments
ONNX_MODEL_PROVIDER=onnx
ONNX_MODEL_PATH=/app/models/phi-4.onnx
ONNX_PORT=8083

# Shared Configuration
E2B_API_KEY=your-e2b-api-key-here
AGENTS_DIR=/app/.claude/agents
ENABLE_LOGGING=true
LOG_LEVEL=info
NODE_ENV=production
MCP_AUTO_START=true
ENABLE_MEMORY=true
MEMORY_TTL=86400

# Load Balancing Strategy
# route_by_task: Choose provider based on task complexity
# round_robin: Distribute evenly
# cost_optimized: Prefer cheaper models
ROUTING_STRATEGY=route_by_task

# Task Routing Rules
COMPLEX_TASKS_PROVIDER=claude          # Architecture, refactoring
SIMPLE_TASKS_PROVIDER=openrouter       # Documentation, research
CODE_TASKS_PROVIDER=deepseek          # Code generation
OFFLINE_PROVIDER=onnx                  # Air-gapped fallback
