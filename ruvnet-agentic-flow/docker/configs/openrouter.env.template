# Agentic Flow - OpenRouter Configuration Template
# Deploy with: MODEL_PROVIDER=openrouter ./docker/cloud-run/deploy.sh
# 99% cost savings compared to Claude!

# Provider Configuration
MODEL_PROVIDER=openrouter
USE_OPENROUTER=true

# Choose your model (cost-effective options)
# Llama 3.1 8B - Fastest, cheapest ($0.0003/task vs $0.08 Claude)
MODEL_NAME=meta-llama/llama-3.1-8b-instruct

# Other popular options:
# MODEL_NAME=deepseek/deepseek-chat-v3.1          # Excellent for code
# MODEL_NAME=google/gemini-2.5-flash-preview      # Very fast
# MODEL_NAME=anthropic/claude-3-5-sonnet          # Same as direct Claude
# MODEL_NAME=openai/gpt-4                         # OpenAI via OpenRouter

# OpenRouter API Key (store in GCP Secret Manager)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Optional: E2B Sandboxes for code execution
E2B_API_KEY=your-e2b-api-key-here

# Agent Configuration
AGENTS_DIR=/app/.claude/agents
ENABLE_LOGGING=true
LOG_LEVEL=info

# Server Configuration
PORT=8080
NODE_ENV=production
PROXY_PORT=3000

# MCP Server Configuration
MCP_AUTO_START=true

# Memory and Coordination
ENABLE_MEMORY=true
MEMORY_TTL=86400
