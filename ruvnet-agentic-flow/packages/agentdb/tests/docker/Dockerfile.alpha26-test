FROM node:20-slim

# Install basic tools
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /test-agentdb

# Copy test script
COPY tests/docker/test-alpha26-simulation.sh .

# Run test
CMD ["bash", "test-alpha26-simulation.sh"]
