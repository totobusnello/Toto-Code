FROM node:22-slim

WORKDIR /test

# Copy just the built package
COPY package.json package-lock.json ./
COPY dist ./dist
COPY .claude ./.claude
COPY README.md LICENSE ./

# Install dependencies
RUN npm ci --only=production

# Test commands
CMD ["node", "dist/cli-proxy.js", "--version"]
