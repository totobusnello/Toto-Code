FROM node:20-slim

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (needed for tsx)
RUN npm install

# Copy source code
COPY src ./src
COPY wasm ./wasm

# Run agent with tsx
CMD ["npx", "tsx", "docker/federation-test/run-agent.ts"]
