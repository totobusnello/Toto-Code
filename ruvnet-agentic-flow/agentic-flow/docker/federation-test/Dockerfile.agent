FROM node:20-slim

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --only=production

# Copy source code
COPY dist ./dist
COPY src ./src
COPY tsconfig.json ./

# Install TypeScript and tsx
RUN npm install -g tsx typescript

# Start agent
CMD ["tsx", "docker/federation-test/run-agent.ts"]
